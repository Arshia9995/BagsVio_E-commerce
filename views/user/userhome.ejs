<%- include('header')-%>
<script>
  $('.carousel').carousel()
  </script>

<%- include('../partials/navbar') %>

<!-- 
<div style="height: 150px;"></div> -->

<!-- Banner Section -->
<!-- <div class="container-fluid p-0 banner-container">
  <img src="/assets/images/b2.jpg" class="img-fluid banner-img" alt="Banner Image">
</div> -->

<!-- First banner section -->

<div class="container-fluid p-0">
  <a href="/products/<%= banners[0].category.categoryName %>"> 
  <img src="/uploads/<%= banners[0].image %>" class="img-fluid banner-img" style=" height: 600px; width: 100%; margin-top: 20px;" alt="Banner Image">
</a>
</div>

<!-- Banner Section -->
<!-- <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <% banners.forEach(function(banner, index) { %>
      <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
        <img class="d-block w-100" src="/uploads/<%= banner.image %>" alt="Slide <%= index + 1 %>">
      </div>
    <% }); %>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>  -->





<!-- New Arrivals Section -->
<section class="container my-5">
  <h2 class="text-center mb-4">New Arrivals</h2>

  <!-- First Row -->
  <div class="row mb-4 align-items-center">
    <!-- Product Cards -->
    <% newarrival.forEach(product => { %>
        <div class="col-lg-4">
          
            <div class="card text-center">
              <div style="text-align: center;">
                <a href="/productdetails/<%= product._id %>">
                <img src="/uploads/<%= product.images[0] %>" class="card-img-top mt-2" style="border-radius: 5px;"  alt="<%= product.ProductName %>">
              </a>
              </div>
                <div class="card-body d-flex flex-column justify-content-center">
                    <h5 class="card-title"><%= product.ProductName %></h5>
                    <p class="card-text"> ₹ <%= product.Price %></p>
                </div>
            
          </div>
        </div>
    <% }) %>
</div>

    
</section>


<!-- Banner Section -->
<!-- <div class="container-fluid p-0 banner-container">
  <img src="/assets/images/b2.jpg" class="img-fluid banner-img" alt="Banner Image">
</div> -->


<div class="container-fluid p-0">
  <% for (let i = 1; i < banners.length; i++) { %>
      <div class="banner-item">
          <a href="/products/<%= banners[i].category.categoryName %> ">
              <img src="/uploads/<%= banners[i].image %>" alt="Banner <%= i + 1 %>" style="height: 600px; width: 100%; margin-top: 10px;">
          </a>
      </div>
  <% } %>
</div>

<!-- Banner Section -->
<!-- <div class="container-fluid p-0 h-75">
    <% if (banners && banners.length > 1 && banners[1].image) { %>
      <img src="/uploads/<%= banners[1].image %>" style="width: 100%;max-height: 700px;" alt="Banner Image">
    <% } %>
  </div> -->




<!-- New Trends Section -->
<section class="container my-5">
  <h2 class="text-center mb-4">New Trends</h2>

  <!-- First Row -->
  <div class="row mb-4 align-items-center">
    <!-- Product Cards -->
    <% newtrends.forEach(product => { %>
        <div class="col-lg-4">
          
            <div class="card text-center">
              <div style="text-align: center;">
                <a href="/productdetails/<%= product._id %>">
                  <img src="/uploads/<%= product.images[0] %>" class="card-img-top mt-2" style="border-radius: 5px;" alt="<%= product.ProductName %>">
                </a>
              </div>
                <div class="card-body d-flex flex-column justify-content-center">
                    <h5 class="card-title"><%= product.ProductName %></h5>
                    <p class="card-text"> ₹ <%= product.Price %></p>
                </div>
            
          </div>
        </div>
    <% }) %>
</div>

    
</section>

<!-- Add this JavaScript code after including Bootstrap's JavaScript -->



<%- include('../partials/user-footer') %>
<%- include('footer')-%>
